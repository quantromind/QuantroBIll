# PetBharke POS Environment Configuration (Production Domain)
# When deployed behind a domain name (e.g. https://pos.petbharke.com),
# all API calls route through the relative /api reverse proxy or domain endpoint:
VITE_API_BASE_URL=/api
